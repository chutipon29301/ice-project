export class User {
    constructor(public nationalID: string, public firstName: string, public lastName: string, public role: string,
         public phone: string, public status: string, public profileImage: string, public credits: number, public totalCredit: number) {}
  }
